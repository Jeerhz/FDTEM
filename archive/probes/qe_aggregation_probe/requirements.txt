# qe_aggregation_probe requirements
#
# The local logic smoke test (`python run.py --smoke`) needs only the light
# stack below the divider. The real run additionally needs the scoring stack,
# and — for perturb.backend: llm — the openai client.
#
# ── scoring (cluster, GPU) ────────────────────────────────────────────────────
torch>=2.0
transformers>=4.34
unbabel-comet>=2.2.0         # CometKiwi / COMET (or use the local repo checkout)
datasets>=2.14               # google/wmt24pp
sentencepiece
openai>=1.30                 # perturb.backend: llm (any OpenAI-compatible endpoint)
# metricx (optional): pip install git+https://github.com/google-research/metricx
# bertalign (optional resegmentation): pip install git+https://github.com/bfsujason/bertalign
# statsmodels (optional mixed-effects): statsmodels>=0.14
# ── fitting / analysis / plots (enough for --smoke) ───────────────────────────
numpy>=1.24
pandas>=2.0
scipy>=1.10
matplotlib>=3.7
pyyaml>=6.0
tqdm>=4.65
