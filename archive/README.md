# Archive

Superseded code, kept for reference. Nothing here is imported by `fdtem/` or
`experiments/`.

| folder | what it was |
|---|---|
| `xglue/` | XGlue probing / finetuning of COMET encoders |
| `probes/` | the Lp-norm QE aggregation probe and the cap-length probe |
| `bio_paragraph_pipeline/` | Bio-MQM paragraph windows + sentence mixes, replaced by the WMT pipeline of experiment 3 |
| `representation_analysis/` | standalone retrieval / alignment / error-sensitivity scripts; their reusable parts now live in `fdtem/` |
| `decks/` | earlier presentation generators |
